# 工业设计AI工作流系统 V4 — 完整架构审核文档

## 请审核方做什么

请从以下角度审核这套系统：
1. 逻辑完整性：步骤之间的数据传递是否有断裂？
2. 可执行性：每一步的指令是否足够清晰，AI能按照执行不歧义？
3. 有效性：这套系统能否达到"10个设计方案客户选中9个"的目标？
4. 冗余性：有没有多余的步骤或机制可以砍掉？
5. 缺失：有没有关键环节遗漏？

---

## 一、系统目标

输入"品类"（+可选客户名），端到端产出5个可交付设计方案（含效果图渲染提示词、PRD、CMF、PPT）。

**核心指标**：客户从5个方案中选中4-5个（80-90%命中率）。

**什么算"选中"**：客户愿意投入开模/打样资源推进这个方案。

---

## 二、设计哲学

### 命中率公式
```
客户选中 = 品牌合规（不会被一眼否掉）
         × 需求回应（看到就知道解决什么问题）  ← 权重最大，60%
         × 视觉吸引力（造型新颖，眼前一亮）    ← 20%
         × 渲染质量（看起来像真的产品）          ← 20%
```

### 人机分工
- **AI的任务**：产出。执行调研、生成概念、写提示词、包装交付。
- **人的任务**：判断。在关键节点做决策（选方向、选概念、评渲染图）。
- AI在每个输出中给出自己的推荐和理由，人来确认或否决。

### 数据流主线（一条线贯穿全流程）
```
消费者痛点（Step 02-04发现）
  → 造型意图：产品必须传达什么视觉信号（Step 04翻译）
  → 参照锚定：哪些现有设计用过类似手法（Step 05检索）
  → 概念定义：组合参照手法+品牌约束=新造型（Step 06生成）
  → 设计深化：精确到可出图的定义（Step 08）
  → 渲染提示词：参考图+差异描述（Step 09）
  → 客户看到：一眼理解这个产品解决什么问题
```

每一步的产出都可以追溯回"这个造型决策回应的是哪个消费者痛点"。追溯不到的造型决策不允许存在。

---

## 三、10步流程

### 流程总览

| 步骤 | 名称 | 核心产出 | 人的介入 |
|------|------|----------|----------|
| 01 | 品类定义 | 品类锁定+认证门槛+运行模式 | 无（自动跑） |
| 02 | 品类市场调研 | 三批执行→市场结论 | 无（自动跑） |
| 03 | 品类设计全景学习 | 三批执行→品类设计谱系图 | 无（自动跑） |
| 04 | 三层需求分析+方向决策 | 需求→造型意图翻译+3方向 | **确认需求+选方向** |
| 05 | 灵感检索 | 从素材库检索造型参照 | 无（自动跑） |
| 06 | 概念生成 | 三阶段造型翻译→6张钩子卡 | **选5个概念** |
| 07 | 概念筛选 | Pugh矩阵+角色分配 | 无（自动跑） |
| 08 | 设计深化 | 7步设计思考链+渲染锚定准备 | 无（自动跑） |
| 09 | 效果图生成与迭代 | 提示词→出图→迭代 | **出图+给反馈** |
| 10 | 交付包装 | PRD+CMF+BOM+PPT | 无（自动跑） |

**人只在3个节点介入**：Step 04选方向、Step 06选概念、Step 09出图反馈。其他全自动。

### 各步骤详细逻辑

#### Step 01-03：品类认知建设（自动执行，一口气跑完）

**Step 01** 锁定品类、认证门槛、运行模式。
**Step 02** 三批数据闸门（采集→验证→结论），所有数字必须有来源引用。
**Step 03** 分析品类内≥8个品牌的设计语言，输出品类设计谱系图（2D坐标+品牌分布+设计语言空白）。

三步之间的数据传递：
- Step 01 → Step 02：品类名+认证要求 → 搜索方向
- Step 02 → Step 03：价格带+渠道+竞品清单 → 品牌选取依据
- Step 03 输出的谱系图 → 后续所有步骤的坐标参照系

#### Step 04：三层需求分析+方向决策（核心翻译层）

**任务一：三层需求分析**

| 层级 | 内容 | 来源 | 置信度 |
|------|------|------|--------|
| 第1层：客户显性需求 | 直接说出来的规格/功能/价格 | brief原文 | HIGH |
| 第2层：客户隐性需求 | 没说但很可能存在的（如想高端化） | 市场数据异常+产品线缺口推断 | 需用户确认 |
| 第3层：消费者痛点 | 使用者行为层面的痛苦 | 用户评论/竞品差评 | 需用户确认 |

**任务二：需求→造型意图翻译（系统最关键的环节）**

对排名前3的消费者痛点，各翻译为1-3条造型策略：

```
痛点："种植灯太重，两个人才能装"
  → 视觉信号：产品必须传达"轻"和"一个人能搞定"
  → 造型策略1：做薄（厚度<40mm，高宽比>1:8）
  → 造型策略2：做模块化（可见的分段逻辑，每段<3kg）
  → 每条策略附带 visual_test："不懂产品的人看到，能说出'这东西很轻'吗？"
  → 每条策略附带检索方向：去素材库搜"极薄产品"
```

**任务三：客户定位+品牌红线**
- 在谱系图上标出客户坐标+设计半径（核心区±1 / 延伸区±2 / 禁区）
- 生成品牌红线表（全流程硬约束）和绿灯表

**任务四：3个设计方向**
- 每个方向必须链接到一个消费者痛点和一条造型策略
- 每个方向有老板7维度预评
- 用户选择1个方向

**数据传递出去**：
- 造型意图翻译表 → Step 05（检索方向）、Step 06（生成依据）
- 品牌红线表 → Step 05/06/07/09（每步都检查）
- 客户设计半径 → Step 06（概念定位检查）

#### Step 05：灵感检索（从素材库，不靠联网）

**输入**：Step 04的造型意图翻译表（告诉系统搜什么）

**执行逻辑**：
1. 读取翻译表中每条造型策略的 `reference_direction`
2. 按优先级从视觉素材库检索：
   - 最高优先：按"解决了什么问题"匹配（DNA卡的 problem_solved 字段）
   - 其次：按造型语言匹配（silhouette + hero_feature）
   - 再次：按可迁移手法匹配（transferable_technique）
3. 素材库不足时联网补充
4. 品牌红线过滤：触犯红线的参照只允许迁移结构逻辑，禁止迁移外形语言

**输出**：5-8张造型参照卡，每张含：
- 参照图片（素材库编号）
- 它解决了什么问题 + 造型怎么回应的
- 我要从它身上迁移什么 + 怎么落地 + 品牌限制

**数据传递出去**：
- 造型参照卡 → Step 06（概念生成的素材）
- 参照图编号 → Step 09（渲染参考图）

#### Step 06：概念生成（三阶段造型翻译引擎）

**阶段A：造型策略展开**
把 Step 04 的造型策略变得更具体——定义几何约束、链接参照、设定验证标准。
输出后等用户确认。

**阶段B：参照组合生成12个概念**（后台执行）
3种生成方法：
- 单策略深挖（4-6个）：同一策略，不同参照展开
- 策略交叉（4-6个）：两条策略组合
- 反向激进（2个）：故意违反某条策略，看有没有意外好方案

每个概念必须包含：
- 来源追溯（哪条策略+哪些参照）
- 层级化定义（基础形态+Hero Feature+功能alibi+如何解决痛点）
- 渲染锚定（用哪张参考图做img2img起点+和参考图的差异）
- visual_test是否通过

**阶段C：多角度评审**（后台执行）
设计师人格库（35人）从"概念生成者"降为"概念评审者"：
- 1个严格型：质疑"有必要这么复杂吗？"
- 1个用户型：质疑"消费者看得懂吗？"
- 1个制造型：质疑"做得出来吗？"
- 1个品类型：按品类专业角度质疑

3轮评审筛到6个，输出6张钩子卡。用户选5个。

**数据传递出去**：
- 5个概念的完整定义 → Step 07/08
- 每个概念的 render_anchor → Step 09

#### Step 07：概念筛选（Pugh矩阵）

自动执行。7维度加权打分（记忆度25%、品牌接受度20%、功能卖点15%、制造可行性15%、成本10%、平台复用5%、合规风险10%）。

硬杀规则：品牌接受度<3分、BOM超10%无降本路径、记忆度<2分 → 退回Step 06重生。

输出：1个冠军+2个安全款+2个边界款。

#### Step 08：设计深化（7步思考链）

每个概念执行：
1. 硬性约束框架（不可动约束+最小包络）
2. 主体块关系（功能体块排列）
3. 整体轮廓和比例（几何语言描述+数字）
4. 分件策略（壳体+工艺+装配）
5. 关键细节（接口/线缆/散热/密封/维护/安装）
6. CMF方向（在品牌DNA范围内）
7. 设计语言总结

**新增第8步：渲染锚定准备**
- 确认参考图是否仍适用
- 写出"和参考图相比的具体差异"（用几何语言）
- 列出3条核心禁止项

**核心规则**：第1-3步（主轮廓/主视觉/功能可见性）为优先项。第4-7步的工程约束不允许削弱前3步的视觉张力。

#### Step 09：效果图生成与迭代

**前置检查**：
- 每个方案的第一视觉特征能14字说清？
- 品牌红线零触犯？

**执行循环**（逐个概念）：

```
系统生成提示词（两种模式）：
  模式A（首选80%）：参考图 + "保留什么/修改什么/禁止什么"
  模式B（备选）：纯文字创意总监brief + 强化禁止项

用户复制提示词到 GPT-4o / Gemini / Nano Banana → 出图

用户反馈 → 系统判断问题层级：
  第1层（造型方向偏）→ 换参考图或重写差异描述
  第2层（细节不对）→ 加禁止项，改局部描述
  第3层（材质不对）→ 只改材质部分
  第4层（风格不对）→ 只改Style+Lighting

每次只改一层，不同时改多层。
满意后 → 出场景图 → 下一个概念。
```

#### Step 10：交付包装

自动执行。输出：
- 5份PRD（每份1页A4）
- 5份CMF规格（绑定到设计区域）
- 5份BOM粗估
- 8页PPT（封面/市场/用户/竞品/方案展示/制造成本/行动建议）

---

## 四、视觉素材库

### 作用
让系统能基于真实的优秀设计做参照，而非凭空编造造型。

### 结构
- **图片存储**：用户桌面 `~/Desktop/设计素材库/`（按来源/品类分文件夹）
- **索引存储**：`.claude/knowledge/素材索引/`（每张图对应一个DNA卡YAML文件）

### DNA卡核心字段
```yaml
image_id: "RD-2024-0847"
category: "outdoor_area_light"
silhouette: "扁平椭圆盘，高宽比约1:5"
hero_feature: "极薄灯体仅28mm，散热翅全部隐藏在背面"
problem_solved: "传统Area Light又厚又丑，建筑师不愿意装"
form_response: "做到极薄（28mm），让灯'消失'在建筑立面上"
transferable_technique: "散热面积做在背面而非侧面"
design_level: 4  # 1-5
```

### 检索优先级
1. 按"解决了什么问题"检索（匹配 problem_solved）— 最高优先
2. 按造型语言检索（匹配 silhouette + hero_feature）
3. 按可迁移手法检索（匹配 transferable_technique）

### 最低可用阈值
主品类 ≥50 张有效DNA卡，跨品类 ≥20 张。低于阈值时系统提示补充。

---

## 五、品牌约束传递机制

Step 04 输出的品牌红线表是**全流程硬约束**，不可绕过：
- Step 05：触犯红线的参照只允许迁移结构逻辑
- Step 06：每轮评审都品牌预筛，触犯红线直接替换
- Step 07：品牌接受度<3分硬杀
- Step 09：红线禁止项写入渲染提示词的"Must NOT appear"

**任何步骤不得以"创意多样性"或"激进探索"为由绕过品牌红线。**

---

## 六、渲染防漂移机制

### 问题
文字描述产品→AI出图时自由发挥→造型和概念定义不一致。

### 解决方案
**模式A（首选）**：给渲染AI一张参考图 + 明确说"保留什么、修改什么、禁止什么"。AI在视觉锚点基础上做修改，而非凭空想象。

**分层迭代**：出图后按4层问题分级，每次只改一层，避免越改越乱。

---

## 七、系统进化机制

### 设计原则
- 不靠AI自评（不可靠）
- 不搞复杂的评分系统
- 利用Claude Code已有的记忆系统
- 通用规则很少改，客户记忆快速积累

### 架构

```
通用系统规则（对所有客户一样，谨慎修改）
  CLAUDE.md + steps/*.md + knowledge/ + templates/

客户记忆（每个客户独立积累，越做越懂）
  通过Claude记忆系统存储，命名规范：
    {客户名}_brand_dna        → 品牌DNA
    {客户名}_preferences      → 设计偏好（喜欢/讨厌什么）
    {客户名}_project_YYYYMM   → 项目结果
```

### 进化触发

**场景1：用户在任何步骤给反馈**
```
用户："这个方向太保守了"
系统："这是这个客户特殊偏好，还是所有项目都应该更大胆？"
  客户特殊 → 存记忆 "{客户名}_preferences"
  通用 → 提出具体规则修改 → 用户批准 → 改规则文件 + git commit
```

**场景2：跨客户模式自动发现**
```
系统在记忆中发现：Agrolux、RAB、Quoizel 三个客户都有"概念太保守"的反馈
  → 主动提出："3个客户都说概念太保守，建议把Step 06反向激进概念从2个改成4个"
  → 用户批准 → 改规则
```

**触发阈值**：同一反馈在 ≥3 个不同客户出现 → 提议改通用规则。3个之前不碰通用规则。

### 客观指标追踪（不评分，只记数）

每个项目结束记录3个硬数字：
1. 每个概念平均渲染迭代次数（越少=系统越准）
2. 被要求重做的步骤（越少=规则越成熟）
3. 客户选中率（北极星指标，客户反馈后补填）

存储在 `artifacts/project_log.yaml`，纯记录，不触发任何自动操作。趋势线由人判读。

### 客户记忆的使用

第二次给同一客户做项目时：
- Step 04 自动加载该客户的 brand_dna 记忆（跳过重新生成，只验证是否过期）
- Step 06 自动读取 preferences（调整激进/保守比例）
- Step 09 自动读取偏好（渲染风格倾向）

越做越快，越做越准。

---

## 八、分析框架库（19个框架）

| 框架 | 步骤 | 模式 | 作用 |
|------|------|------|------|
| F01 | 04 | Always-on | 客户产品线匹配+品牌DNA包 |
| F02 | 01 | Always-on | 认证门槛粗筛 |
| F03 | 02 | Default | 渠道×价格带地图 |
| F04 | 01/02 | Default | 品类吸引力矩阵 |
| F05 | 02 | Conditional | 价值链利润分布 |
| F06 | 03 | Default | 竞争空白分析 |
| F07 | 03-04 | Default | 空白需求验证 |
| F08 | 04 | Default | B2B用户决策路径 |
| F09 | 04/08 | Always-on | 平台/模块复用匹配 |
| F10 | 04/07/08 | Always-on | 目标成本走廊 |
| F11 | 01 | Conditional | PESTLE宏观环境 |
| F12 | 04 | Conditional | Kano功能分层 |
| F13 | 05 | Default | 类比迁移筛选 |
| F14 | 06 | Always-on | 概念覆盖矩阵 |
| F15 | 07 | Always-on | Pugh概念筛选矩阵 |
| F16 | 09 | Always-on | 出图锁定清单 |
| F17 | 10 | Always-on | Claim-Evidence-Risk矩阵 |
| F18 | 03 | Always-on | 品类设计语言分析 |
| F19 | 04 | Always-on | 需求→造型意图翻译 |

---

## 九、已知局限

1. **素材库冷启动**：素材库需要 ≥50 张有效DNA卡才能正常支撑Step 05/06，目前还在建设中
2. **品类限制**：知识库（灯具设计大师库）目前只覆盖灯具，其他品类需要扩展
3. **第一个项目无基线**：没跑过真实项目，系统效果未验证
4. **渲染质量依赖外部AI**：GPT-4o/Gemini的出图能力是系统无法控制的变量
5. **Step 01-03 联网数据获取不稳定**：AI搜索能力有限，可能搜不到准确数据

---

## 十、审核问题清单

请审核方回答以下问题：

1. Step 04 的"需求→造型意图翻译"环节，是否真的能保证每个概念都回应消费者痛点？有没有可能翻译本身出错导致后续全错？
2. Step 06 的三种生成方法（单策略深挖/策略交叉/反向激进），数量分配（4-6/4-6/2）是否合理？
3. 设计师人格从"生成者"降为"评审者"，会不会丢失创造力？
4. 素材库检索机制能否真正替代人类设计师"逛Pinterest获取灵感"的过程？
5. 渲染防漂移的模式A（参考图+差异描述）在实际操作中是否可行？GPT-4o/Gemini能否准确执行"保留这些，修改那些"？
6. 进化机制（记忆+跨客户模式发现）是否足够？还是需要更主动的自我优化？
7. 整个系统的复杂度是否过高？有没有可以大幅简化的地方？
8. 3个人工介入节点（选方向、选概念、出图反馈）是否足够？还是需要更多/更少？
