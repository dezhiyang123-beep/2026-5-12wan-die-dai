# 工业设计AI工作流系统 V4.6.1

## 严谨性准则（最高优先级，覆盖所有行为）

1. **不说"不影响"/"无所谓"/"可以不处理"**。发现问题就处理，不分大小。未处理的问题会累积成系统性风险。
2. **DNA卡和图片必须一一对应**。删了DNA卡条目就删对应图片，删了图片就删对应DNA卡条目。不允许出现孤立的DNA卡（指向不存在的图）或孤立的图片（没有DNA卡索引）。
3. **审核必须读图验证**。不能只看YAML文本就说"通过"。每次审核至少抽查3张图，打开图片对照DNA卡描述。
4. **发现数据质量问题立即处理**。不要标记"待人工确认"然后跳过。能自己判断的立即处理，确实需要用户判断的明确列出并追问。
5. **不做覆盖率估算，做精确计数**。说"大概84%覆盖"不如说"584张图中494张有DNA卡，90张缺失，缺失的子品类是：xxx"。
6. **commit前检查一致性**。DNA卡中的source_path必须指向真实存在的图片文件。每次commit前跑一遍路径验证。
7. **不留残留文件**。旧文件被新文件取代时，立即删除旧文件。不要留着"以后再清理"。
8. **指令给其他CC/Codex时，把完整规则写进去**。不要假设对方"应该知道"。schema格式、score范围、文件路径、不要中途停下来——全部写明。
9. **版本管理**：每次改动步骤文件/框架库/模板，版本号递增（主改动+0.1，小修+0.0.1）。commit消息中必须包含版本号。当前版本：V4.6.1。
10. **【R20】所有数字字段必须有evidence_source标注**。市场规模、价格带、BOM/成本走廊、认证费用、几何尺寸、痛点频次——凡是数字，必须标注来源（"工厂报价"/"Alibaba询盘"/"Step 01 CL-XX"/"AI推断（推理链：___）"）。AI推断的数字不能直接作为硬约束传递；BOM类数字必须在Step 04经至少一个OEM报价参照才能进入Step 05 Pugh。
11. **【R21】FACT级标注必须有原始来源URL或文档摘录**。"来源：行业经验"或"来源：AI推断"不能标FACT。FACT标错比推理飘移更危险——错误FACT会以硬约束形式传递到整个项目。已知易错事实见 `.claude/knowledge/已知事实修正库.yaml`，标FACT前必须对照检查。
12. **【R2】枚举值必须合法**。所有verdict/status/evidence_level字段只能使用预定义的枚举值。发明"CONDITIONAL"/"PARTIAL"/"WEAK_PASS"/"PASS_PREDICTED"等非法值 → 该自检项强制FAIL，不允许进入下一步。合法枚举见各步骤文件的枚举schema定义。

## 写代码时的行为准则（Karpathy Guidelines）
1. **先想再写**：不假设、不藏困惑。不确定就问。
2. **极简优先**：只写解决问题的最少代码。不加没要求的功能。
3. **精准修改**：只动该动的。不顺手改旁边的代码。
4. **目标驱动**：先定义成功标准，执行后验证是否达标。
完整版见 `KARPATHY.md`。

## 新接手须知（换电脑/新对话时读这里）

你正在接手一套工业设计AI工作流系统。

**做什么**：输入品类+客户名 → 端到端产出5个工业设计方案（含效果图渲染提示词、PRD、CMF、PPT）。

**核心文件**：
```
CLAUDE.md                        ← 主控文件（你在读的）
.claude/steps/00-10              ← 11个步骤的执行规则
.claude/knowledge/               ← 框架库 + 设计手法库 + 素材库规范 + 素材索引 + 反例库
.claude/templates/               ← 输出模板
.claude/clients/                 ← 客户记忆（品牌语法、偏好、项目历史）
.claude/启动提示词.md             ← 用户启动项目时的提示词
.claude/交接指令.md               ← 完整交接说明
.claude/project_log.yaml         ← 项目指标追踪
```

**人机分工**：AI产出，人判断。3个决策断点：Step 03选方向、Step 05选概念、Step 08出图反馈。其他步骤有确认检查点（选择题模式），但不需要用户主动决策。
**客户记忆**：存在 `.claude/clients/{客户名}/`，跟git走。
**进化机制**：反馈→客户记忆→跨客户模式发现→规则改进。
**素材库**：图片在用户桌面 `~/Desktop/设计素材库/`，索引在 `.claude/knowledge/素材索引/`。

---

## 系统目标

输入品类+客户名 → 5个可交付设计方案。

**北极星指标**：
- 新客户首次：5选2-3（40-60%）
- 有项目记忆：5选3-4（60-80%）
- 长期客户：5选4（~80%）

## 运行模式

所有项目走完整11步，不跳步。已有客户记忆时Step 03自动加载已有品牌语法（不用重新提取），但不跳过任何步骤。

**【R17】测试模式 vs 生产模式区分**：

| 行为 | 测试模式（audit/ID-check触发） | 生产模式（正式项目） |
|------|-------------------------------|---------------------|
| 客户记忆写入 | 禁止写入（只读） | 必须写入（Step 03/10） |
| 客户偏好加载 | 加载但标注"测试模式，偏好供参考" | 必须加载并严格执行 |
| C/D级数据 | 允许作为假设输入继续流程 | 必须标注风险，不能作硬约束 |
| Step 10复盘 | 不执行（不写入生产记忆） | 必须执行 |
| 反例库写入 | 仅写入临时test_run目录 | 直接写入正式反例库 |

触发条件：用户明确说"测试"/"审计"/"ID-check"，或通过skill audit/ID-check-skill触发，或brief包含"test_brief"/"audit_brief"标注。

## 全流程强制完成

- 禁止在Step 08或更早停止。用户可以要求暂停，但系统必须标注"未完成"并记录停在哪一步。
- Step 09（PRD+CMF+BOM+PPT）和Step 10（复盘）是项目完成的必要条件。
- 没有Step 10复盘的项目不写入客户记忆。

## 步骤间产出规范

- 每步输出必须引用对应的模板（如有）。自由文本产出不被接受。
- 每步结束前必须做产出完整性自检：列出本步要求的所有产出项，逐项确认已完成。缺一项不允许进入下一步。
- 每步开头必须列出本步调用的框架清单（从分析框架库.md中提取），未调用的框架必须记录跳过原因。

## 流程概览（11步）

| 步骤 | 名称 | 核心产出 | 人介入？ |
|------|------|----------|----------|
| 00 | Brief检查 | 信息完整性+客户记忆加载 | 缺信息时问+加载历史经验 |
| 01 | 品类扫描 | 认证+市场+价格+成本走廊+反面证据+**客户现有产品扫描** | 无 |
| 02 | 品类骨架建模 | 品类器官图+三层分类（身份锚点/强约束/可创新区域）+假设清单 | 选择题确认 |
| 03 | 品牌语法+需求翻译 | 品牌语法5码（含证据等级）+三层需求+造型意图翻译+3方向 | **选择题确认+选方向** |
| 04 | 精准迁移 | 同品类主锚点+跨品类局部迁移+4项校验+迁移合法性验证+反例+使用场景建模 | 无 |
| 05 | 概念生成与筛选 | 16概念（6种方法）→快筛→8角色评审→8出给用户→选6→Pugh→5方案 | **选概念** |
| 06 | 视觉总监审查 | 品牌距离+创新距离双轴评分+视觉协调性+记忆点验证 | 无 |
| 07 | 设计深化 | 7步思考链+概念完整性校验+3张渲染参考图锚定 | 无 |
| 08 | 渲染控制与迭代 | 3张参考图+7段提示词→出图→4层迭代 | **出图+反馈** |
| 09 | 交付包装 | PRD+CMF+BOM+PPT | 无 |
| 10 | 项目复盘 | 指标记录+反馈分类+客户记忆更新+反例入库+规则沉淀 | 反馈分类确认 |

**3个决策断点**：Step 03选方向、Step 05选概念、Step 08出图反馈。其他步骤的确认检查点为选择题模式，不阻断主流程。

**【R14】决策断点 vs 确认检查点定义**：
- **决策断点（Decision Gate）**：AI不能独立前进，必须等用户作出选择后才能继续。共3个：Step 03选方向、Step 05选6个概念、Step 08出图反馈。
- **确认检查点（Confirmation Checkpoint）**：AI产出后呈现给用户确认，选A=继续，选B=调整，选C=人工干预。用户未回应则默认继续（超时30分钟后AI可自行继续）。出现在Step 01/02/04。
- **[DESIGN_CONFLICT]标注**：Step 07中如果工程约束和视觉张力无法兼容，标注[DESIGN_CONFLICT]后升级为用户决策——这是第4类决策点，触发频率低但性质等同于决策断点。
- 以上分类不能混淆：确认检查点不等于决策断点，系统在确认检查点有权推进，在决策断点必须等待。

## 数据流主线

```
消费者痛点（Step 01-03发现）
  → 品类骨架约束（Step 02：知道什么不能动）
  → 品牌语法约束（Step 03：知道怎么变仍然像它）
  → 造型意图翻译 + 三重校验（Step 03：核心防错点）
  → 精准迁移（Step 04：同品类主骨架+跨品类局部增强+迁移验证）
  → 概念生成 6种方法 + 视觉总监快筛（Step 05）
  → 视觉总监终审 + 品牌/创新双轴评分（Step 06）
  → 设计深化 + 完整性校验（Step 07）
  → 3张参考图渲染控制 + 4层迭代（Step 08）
  → 交付（Step 09）
  → 复盘进化（Step 10）
```

## 四层素材库

| 层 | 回答什么 | 示例 |
|---|---|---|
| 品类标杆库 | 该品类最好的产品长什么样？ | 电钻：DeWalt/Milwaukee/Makita标杆款 |
| 品牌语法库 | 该品牌的稳定设计规律？ | DeWalt 20款产品→提取语法 |
| 设计手法库 | 某种手法在哪做得最好？ | "包胶防护"→DeWalt+GoPro+户外手电 |
| 失败反例库 | 什么不能学？ | 学了就廉价/超成本/撞脸/不像品牌 |

同一张图可以同时属于多层。图片不动，索引方式变。

## 推理审计层（V4.5+，V4.6.1强化）

**F20证据到结论审计是整个系统的质量闸门。**

核心规则：
1. 战略工具（F21-F24）产生视角，F20负责裁决——工具不做判断，审计做判断
2. 禁止从数据直接跳到结论——必须有可追溯的推理链
3. 禁止无标准打分——所有1-5分评分必须有对应的评分标准表
4. 禁止假设包装成事实——每个判断必须标注Status（FACT/INFERENCE/HYPOTHESIS/USER_SIGNAL/NEEDS_VALIDATION）
5. 禁止空白=机会——市场空白、设计空白、竞品空白都必须经F20审计后才能判定为机会

信号区分规则：
- 客户正式需求（如RFQ/brief/合同）→ 标注为client_explicit，evidence_level A
- 用户反馈信号（如live_memory中的"太保守了"）→ 标注为USER_SIGNAL，不等同于客户正式需求
- 竞品观察→ 标注为competitor_observation，evidence_level C
- AI推断→ 标注为ai_inference，必须附推理链，evidence_level D

## 精准度控制层（V4.3→V4.5.1持续强化）

### 品类骨架（Step 02）
三层分类：
- **身份锚点**：动了就不像该品类 → 禁止挑战
- **强约束**：可微调但不能随便改 → 谨慎挑战，需证明收益
- **可创新区域**：设计自由度最大

关键原则：**锁功能关系，不锁具体位置。**

### 品牌语法（Step 03）
5码：表面码+比例码+结构码+语义码+禁止码。
每条标注证据等级（A/B/C/D），C/D不能单独做淘汰依据。
系统自动提取，用户选择题确认。

### 品牌距离+创新距离双轴评分（Step 06）
品牌距离1-5 + 创新距离1-5。
最优：品牌4 + 创新3-4。品牌<3直接淘汰。

**品牌距离评分锚点**（每分必须附具体产品对比）：
- 1分："完全不像" → 说明像哪个品牌，和客户品牌哪个元素冲突
- 2分："气质偏离" → 说明哪个元素保留了，哪个偏了
- 3分："基本像" → 说明和品牌哪款现有产品最接近
- 4分："像新产品" → 说明造型语言是自然延伸还是跳跃
- 5分："像下一代旗舰" → 说明比品牌当前最好的产品在哪个维度前进了一步

不附具体对比依据的评分无效。

### 迁移合法性（Step 04）
7项验证：借什么/为什么能迁移/用户能理解吗/品牌合规/成本/制造/抄袭风险。
跨品类4项校验：约束相似+触点相似+语义相似+制造相似。
**主骨架必须来自同品类，跨品类只做局部增强。**

## 概念生成6种方法（Step 05）

| 方法 | 数量 | 安全/冒险 |
|------|------|-----------|
| 单策略深挖 | 4 | 安全 |
| 策略交叉 | 4 | 安全 |
| 反向激进 | 2 | 中等 |
| 细节拼接 | 2 | 中等（最多2个外来细节，统一设计动词） |
| Wildcard | 2 | 冒险 |
| 品类假设挑战 | 2 | 最大突破潜力（只挑战惯例，不挑战身份锚点） |

### 品类假设挑战安全阀（6项全通过才保留）
1. 挑战后用户还能认出品类？ 2. 核心功能没变弱？ 3. 操作动作仍成立？ 4. 品牌语法合规？ 5. 用户体验变好？ 6. 只挑战了一个假设？

### 5方案市场角色
冠军款（品牌4+创新3-4）/ 安全款1（最接近现有线）/ 安全款2（成本最优）/ 边界款1（品牌内新意）/ 边界款2（可放品类假设挑战）
Wildcard最多保留1个，冠军不能来自Wildcard。前3个项目品类假设挑战不做冠军。

### 购买句验证
每个概念必须说出："我们这款产品比普通产品更____，因为你一眼能看到____。"

### Hero Claim验证
去掉某个特征后方案是否立刻变普通？是 → 该特征是灵魂。否 → 方案没有灵魂。

## 8角色评审

| 角色 | 焦点 |
|------|------|
| 品牌守门人 | 品牌合规 |
| 用户理解者 | 消费者能否3秒理解 |
| 工业设计师 | Hero Feature够强吗 |
| 制造工程师 | 做得出来吗 |
| 成本经理 | BOM在走廊内吗 |
| 渠道代表 | 现有渠道能卖吗 |
| 品类专家 | 品类专业问题（按品类动态匹配） |
| **视觉总监** | 轮廓记忆点+主次关系+比例+拼接感+品牌距离 |

视觉总监有否决权。

## 渲染控制（Step 08）

### 3张参考图（优先级递减）
1. 品类骨架参考（highest）：控制不跑品类
2. 品牌风格参考（high）：控制品牌气质
3. 局部细节参考（medium）：控制Hero Feature

### 7段提示词结构
KEEP / CHANGE / MATERIAL / CAMERA / QUALITY / MUST NOT + 参考图

### 4层迭代
造型方向→细节→材质→风格，每次只改一层。

## 品牌约束传递
品牌红线检查四问（Step 04/05/08每步必答）：
1. 违反红线了吗？ 2. 为创新扩大了创新半径吗？ 3. 出现品牌从没用过的视觉语言了吗？ 4. 像另一个品牌了吗？

## 框架体系（25个，三层结构）

### 系统级必跑（7个）
- **F20 证据到结论审计** — 推理质量总闸门，所有关键判断必须经F20审计
- **F25 购买理由审计** — 商业可行性总闸门，6角色审计（按品类动态匹配角色）
- F01品牌语法、F02认证、F10成本、F18品类设计语言、F19需求翻译

### 步骤级必备（6个）
F09平台复用(Step04)、F13迁移筛选(Step04)、F14概念覆盖(Step05)、F15 Pugh(Step05)、F16出图锁定(Step08)、F17 CER(Step09)

### 条件触发（12个）
F03-F08、F11-F12、F21-F24。触发条件满足时必须跑——跳过=调研质量降低。

## 设计手法库（12种手法，H01-H12）
详见 `.claude/knowledge/设计手法库.md`。
Step 04检索素材时可按手法检索，Step 05每个概念标注主手法+辅手法。
H01减法、H02包覆、H03分件线、H04参数化、H05模块化、H06仿生、H07极简整合、H08对比强调、H09隐藏工程、H10材料表达、H11功能外显、H12人机界面强化。
每个概念最多2种主手法，手法选择必须服务造型策略和成本走廊。

## 系统进化机制

### 客户记忆（.claude/clients/）
```
{客户名}/
├── brand_grammar.yaml    ← 品牌语法5码
├── preferences.yaml      ← 偏好
└── project_history.yaml  ← 项目结果
```

### 规则变更三档
- 通用：≥3客户重复 → 改
- 品类：同品类2次 → 改
- 致命：1次严重失败 → 立刻加红线

### 项目复盘（Step 10）
每个项目结束必须执行：指标记录→反馈分类（选择题）→客户记忆更新→反例入库→规则变更检查。

### 素材入库节奏
前20张每5张校对 → 21-100每10张抽查3张 → 100+每20张抽查5张。
抽查看4项：描述准确性/细节可迁移性/标签正确性/品牌语义准确性。
