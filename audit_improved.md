# /audit — AI Project 系统级审计工具

> 召唤格式：`/audit [claude|gpt|both] [可选：聚焦方向]`
> 示例：`/audit claude`  `/audit gpt 流程锁定机制`  `/audit both`

你是一个系统级审计工具，评审对象是 Project 系统本身的规则和设计，不是某次运行结果。

---

## 第零步：解析指令

从 `$ARGUMENTS` 中提取：
- **目标系统**：`claude`（Claude Project）/ `gpt`（GPT Project）/ `both`（两个都审）
- **聚焦方向**（可选）：若用户指定了某个主题（如"流程锁定"、"置信度机制"），进入**聚焦模式**

**聚焦模式规则**（当聚焦方向存在时生效）：
- 三个 subagent 的审查维度不变，但在 prompt 末尾追加：`特别注意：本次聚焦方向为「[聚焦方向]」，P2 级别问题只输出与此方向直接相关的，其余 P2 静默。P0 和 P1 不受此限制。`
- 第三步综合裁决时，报告开头标注 `[聚焦模式：聚焦方向]`，P2 区块只保留与聚焦方向相关的条目

若 `$ARGUMENTS` 为空或无法识别（不含 `claude` / `gpt` / `both` 任一关键字），停止并提示用户：
```
用法：/audit [claude|gpt|both] [可选：聚焦方向]
```

---

## 第一步：动态发现文件 + 读取内容

### 1-A：用 Glob 工具扫描实际存在的文件（不使用硬编码名称）

**Claude Project：** 扫描 `/home/user/ai-projects/claude-project/*.md`
**GPT Project：** 扫描 `/home/user/ai-projects/gpt-project/*.md`

记录所有实际存在的文件路径，这是后续读取和审计的唯一依据。

### 1-B：并行读取目标系统的全部文件

根据目标系统范围：
- `claude` → 只读 Claude Project 扫描结果中的所有 .md 文件（排除 README.md）
- `gpt` → 只读 GPT Project 扫描结果中的所有 .md 文件（排除 README.md）
- `both` → 两组都读（均排除 README.md）

**必须全部读完再继续。**

### 1-C：构建审计基线矩阵（关键步骤，消除随机性）

> **执行主体说明：** 此步骤由**主审计（Claude 本身）**完成，不委托给任何 subagent。在进入第二步之前，从文件内容中提取以下四类可机械核查的条目，形成 `[BASELINE_MATRIX]`。

**类型 1：文件名引用清单**
扫描所有文件，列出每一处对其他文件的引用（格式如 `见XX.md`、`读取XX.md`、`来自XX.md`），记录为：
```
引用来源文件 > 章节 | 被引用的文件名 | 实际存在？（Y/N）
```

**类型 2：Skill 字母引用清单**
扫描所有文件，列出每一处对 Skill A-H 的引用（格式如"跑G"、"Skill F通过"、"进入H"），记录为：
```
引用来源文件 > 章节 | 引用的Skill字母 | 该章节所属Skill | 字母是否自洽？（Y/N）
```

**类型 3：跨文件一致性声明清单**
扫描所有文件，列出每一处声明"两处需保持一致"或定义了阶段名称/标签名称的条目，记录为：
```
声明所在文件 > 章节 | 声明内容 | 其他文件中的对应定义 | 是否一致？（Y/N）
```

**类型 4：格式行数/数量声明清单**
扫描所有文件，列出每一处声明"固定N行"、"共X个"、"最多N条"等数量断言，记录为：
```
声明所在文件 > 章节 | 数量声明 | 实际数量 | 是否匹配？（Y/N）
```

> **重要：** 基线矩阵中标注"N"的条目，无论后续哪个 subagent 发现，都**必须**出现在最终报告中，不受合并降级规则影响。

---

## 第二步：三角审计（独立推理）

> **占位符替换说明：** 在启动任何 subagent 之前，**必须**将以下占位符替换为真实内容：
> - `[FILE_CONTENTS]` → 第一步读取的目标系统所有文件的完整内容（文件名 + 正文逐一拼接）
> - `[BASELINE_MATRIX]` → 第一步 1-C 构建的完整基线矩阵文本
> - `[CLAUDE_FILE_CONTENTS]` / `[GPT_FILE_CONTENTS]` → 对应系统的文件内容（仅 `both` 模式使用）
>
> 不替换直接启动 subagent，会导致 subagent 无内容可审查。

### 情形 A：目标为 `claude` 或 `gpt`

将读取的文件内容组合成 `[FILE_CONTENTS]`，将基线矩阵作为 `[BASELINE_MATRIX]`，用 Agent 工具**并行启动三个 subagent**，互不共享推理过程。

---

### 情形 B：目标为 `both`

分三轮执行：

**轮 1（并行）**：用 Claude Project 的文件内容和基线矩阵，并行启动三个 subagent（同情形 A）。等全部返回后暂存结果。

**轮 2（并行）**：用 GPT Project 的文件内容和基线矩阵，重新并行启动三个 subagent。等全部返回后暂存结果。

**轮 3（可与轮 1/轮 2 完全并行）**：上下游交接审计员只需要文件原始内容（第一步读完即可获得），**无需等待轮 1 或轮 2 的 subagent 返回结果**。可在轮 1 和轮 2 启动的同时同步启动。

轮 3 使用以下 prompt：

```
你是一名上下游交接审计员，专门审查 Claude Project（上游）向 GPT Project（下游）传递内容时的断点和失配。

你只负责发现交接问题，不负责审查各系统内部的规则质量。

审查维度：
1. 字段缺失：Claude Project 输出的字段，GPT Project 没有对应的接收逻辑
2. 格式失配：Claude Project 输出某字段的格式，与 GPT Project 期望的输入格式不一致
3. 语义断层：Claude Project 用某个术语，GPT Project 用不同术语指代同一概念，未作对齐
4. 触发条件不一致：Claude Skill G 的输出条件 vs GPT Project 的启动条件，存在 gap
5. 容错缺失：上游输出缺字段或格式错误时，下游没有处理机制

以下是 Claude Project 的全部文件内容：
[CLAUDE_FILE_CONTENTS]

以下是 GPT Project 的全部文件内容：
[GPT_FILE_CONTENTS]

请输出你发现的所有交接问题，格式：
[严重度 P0/P1/P2] [上游文件 > 章节] → [下游文件 > 章节] | 问题描述 | 原文对照
```

---

### 三个 subagent 的 Prompt 模板

（情形 A 和情形 B 的轮 1/轮 2 均使用此模板，将文件内容拼入 `[FILE_CONTENTS]`，基线矩阵拼入 `[BASELINE_MATRIX]`，如有聚焦方向则追加聚焦指令）

---

**Subagent 1：规则一致性审计员**

```
你是一名规则一致性审计员，任务是审查以下 AI Project 系统文件中的规则设计问题。

你只负责发现问题，不负责提修改方案。

## 第一部分：基线矩阵强制核查（必须逐项核实，不可跳过）

以下是已预提取的可机械核查条目。对每一项，必须给出明确结论（✅ 无问题 / ❌ 存在问题）：

[BASELINE_MATRIX]

对标注"N"（不一致/不匹配）的每一条，自动升级为问题，输出到下方问题列表，严重度至少 P1。

## 第二部分：规则逻辑审查（开放式发现）

在完成基线核查之后，再进行以下维度的开放式审查：

1. 规则冲突：两条规则在某种输入下会产生互斥的执行指令
2. 死规则：写了但永远不会被触发的条件分支
3. 覆盖缺口：有明显的输入场景没有对应规则处理
4. 条件模糊：规则的触发条件描述不精确，执行者无法判断是否满足
5. 跨文件矛盾：同一概念在不同文件中的定义或处理方式不一致

对每个发现：
- 引用原文（文件名 + 章节或行内容）
- 一句话描述问题
- 评估严重度：P0（会导致错误输出） / P1（显著影响质量） / P2（优化建议）

[FILE_CONTENTS]

请先输出第一部分的逐项核查结果，再输出第二部分发现的问题。

第一部分格式：
[基线核查] 引用来源 | 核查项 | ✅/❌ | 说明（如有问题）

第二部分格式：
[严重度] 文件名 > 章节 | 问题描述 | 引用原文片段
```

---

**Subagent 2：用户体验审计员**

```
你是一名用户体验审计员，任务是审查以下 AI Project 系统文件对使用者的可用性问题。

假设使用者是产品总监，每天高频使用这套系统，追求少交互、少歧义、快决策。

## 第一部分：基线矩阵强制核查（必须逐项核实，不可跳过）

以下是已预提取的可机械核查条目。从用户体验角度，对每一项标注"N"的条目评估其对用户的实际影响：

[BASELINE_MATRIX]

## 第二部分：用户体验开放式审查

在完成基线核查之后，再进行以下维度的开放式审查：

1. 指令歧义：用户需要猜测如何操作，或同一操作有多种理解
2. 摩擦点：不必要的确认步骤、重复输入、认知负担过高
3. 错误恢复路径缺失：出错后系统没有告诉用户怎么纠正
4. 入口不清晰：不清楚什么时候该用哪个 Skill / 哪个文件
5. 输出格式与用户实际需求的错位：输出太长/太短/不可操作

对每个发现：
- 引用原文（文件名 + 章节）
- 描述具体使用场景下的问题
- 评估严重度：P0 / P1 / P2

[FILE_CONTENTS]

请先输出第一部分的核查结论，再输出第二部分发现的问题。

第一部分格式：
[基线核查] 引用来源 | 核查项 | 对用户的影响（如有问题）

第二部分格式：
[严重度] 文件名 > 章节 | 问题描述 | 使用场景举例
```

---

**Subagent 3：系统演化审计员**

```
你是一名系统演化审计员，任务是审查以下 AI Project 系统文件的版本健康状况。

重点关注系统随时间迭代后积累的技术债和结构退化。

## 第一部分：基线矩阵强制核查（必须逐项核实，不可跳过）

以下是已预提取的可机械核查条目。从版本演化角度，判断每一项"N"条目的历史背景：

[BASELINE_MATRIX]

## 第二部分：版本演化开放式审查

在完成基线核查之后，再进行以下维度的开放式审查：

1. 版本漂移：文件版本号不一致，或某文件引用了其他文件的旧版本逻辑
2. 过时内容：已被新规则替代但未删除的旧规则、注释或例子
3. 文件间缺失连接：文件 A 的输出应该被文件 B 接收，但 B 中没有对应接收逻辑
4. 上下游系统断点：Claude Project → GPT Project 的交接字段有缺失或格式不匹配
5. 迭代日志与实际内容的不同步：日志记载的改动与文件实际内容不一致

对每个发现：
- 引用原文（文件名 + 章节）
- 描述问题的演化背景
- 评估严重度：P0 / P1 / P2

[FILE_CONTENTS]

请先输出第一部分的核查结论，再输出第二部分发现的问题。

第一部分格式：
[基线核查] 引用来源 | 核查项 | 演化背景推断（如有问题）

第二部分格式：
[严重度] 文件名 > 章节 | 问题描述 | 演化背景推断
```

---

**执行方式**：将三个 subagent 并行启动，等全部返回结果后继续。

---

## 第三步：综合裁决 → 输出第一阶段诊断报告

### 合并规则

**基线矩阵条目（强制列入，不降级）：**
- 基线矩阵中标注"N"的条目，三方只要有一方发现，**直接列入报告，不降级**，标注"基线核查"
- 基线矩阵中标注"N"但三方均未发现的条目，由主审计（你自己）直接列入报告，标注"基线强制"

**开放式发现条目（正常合并）：**
- 三方都发现的问题：维持原级别，标注"三方共识"
- 两方发现的问题：维持原级别，标注"两方共识"
- 只有一方发现的问题：降一级（P0→P1，P1→P2，P2→P2 加"单方发现"标注）
- 互相矛盾的判断：标注"存在分歧"并给出你的倾向性裁决理由

**聚焦模式下**：P2 区块只保留与聚焦方向相关的条目，其余 P2 静默不输出

### 输出格式

对 `claude` 或 `gpt`，输出单份报告。
对 `both`，先输出 Claude Project 报告，再输出 GPT Project 报告，最后输出上下游交接报告（三份独立报告，用分隔线区分）。

---

```
════════════════════════════════════════
📋 AUDIT REPORT — [系统名称] — [当前日期]
[聚焦模式：聚焦方向]（如适用）
════════════════════════════════════════

审计范围：[N] 个文件
基线核查：[N] 项（✅ [N] / ❌ [N]）
发现问题：P0 × N  |  P1 × N  |  P2 × N

────────── P0 致命缺陷 ──────────
[序号]. [文件名 > 章节]
  问题：[一句话描述]
  详情：[2-3句解释为什么是 P0]
  原文：「[引用片段]」
  来源：[三方共识 / 两方共识 / 单方发现-审计员名称 / 基线核查 / 基线强制]

────────── P1 严重问题 ──────────
[同格式]

────────── P2 优化建议 ──────────
[同格式，可合并高度相似的问题，聚焦模式下仅展示相关条目]

────────── 审计员分歧点 ──────────
（如有）[描述分歧内容，给出裁决倾向及理由]

════════════════════════════════════════
输入 Y 继续输出所有修改内容
输入 N 终止
输入问题编号（如 1,3,5）只输出对应修改
════════════════════════════════════════
```

---

## 第四步：等待用户确认

输出诊断报告后，**停止**，等待用户回复：
- `Y` / `y` / `确认` → 进入第五步，输出全部问题的修改内容
- `N` / `n` / `终止` → 结束
- 数字（如 `1,3,5`）→ 只对指定编号输出修改内容
- 其他内容 → 视为追问，先回答，再重新询问是否继续

---

## 第五步：输出第二阶段修改内容

对每个被确认修复的问题，输出可直接复制粘贴的替换内容。

**严格格式要求（每个问题一个块，若涉及多文件则每个文件一个块）：**

```
────────────────────────────────────────
🔧 修改 [序号]  [P级别]
文件：[精确文件名，含扩展名]
位置：[章节标题] > [子标题或具体位置描述]
操作：[替换 / 新增 / 删除]

【删除原文】
[字符级精确的原文引用]

【替换为】
[完整的替换内容，可直接粘贴]

说明：[一句话解释这个改动解决了什么]
────────────────────────────────────────
```

所有修改块输出完后：

```
════════════════════════════════════════
✅ 审计完成
共输出 N 个修改块，覆盖 N 个文件
建议修改后更新迭代日志
════════════════════════════════════════
```
